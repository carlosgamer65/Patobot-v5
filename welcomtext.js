const welcome = (number, groupname) => {
    return `Hola @${number}. Bienvenido al grupo ${groupname} presentate con foto o te viola tu tio`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adios putona @${number}. 👋`
}
exports.bye = bye
