const welcome = (number, groupname) => {
    return `Hola @${number}. Bienvenido al grupo ${groupname} presentetae con foto o te viola tu tio`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adios puta, te espero en 4 @${number}. 👋`
}
exports.bye = bye
